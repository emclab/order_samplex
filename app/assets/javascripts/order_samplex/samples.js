// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
$(function() {
   $("#sample_date_oked").datepicker({dateFormat: 'yy-mm-dd'});
   $("#sample_delivery_date").datepicker({dateFormat: 'yy-mm-dd'});
   $("#sample_shipping_date").datepicker({dateFormat: 'yy-mm-dd'});
});